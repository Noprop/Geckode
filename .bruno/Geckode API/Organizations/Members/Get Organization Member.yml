info:
  name: Get Organization Member
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{organizationsAPI}}1/{{membersPath}}2/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
