info:
  name: Create Ban
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}{{organizationsAPI}}1/{{bannedMembersPath}}"
  body:
    type: json
    data: |-
      {
          "user_id" : 2,
          "ban_reason" : "you suck"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
