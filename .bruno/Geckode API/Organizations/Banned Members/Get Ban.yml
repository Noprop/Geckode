info:
  name: Get Ban
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{organizationsAPI}}1/{{bannedMembersPath}}1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
