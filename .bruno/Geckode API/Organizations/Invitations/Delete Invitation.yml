info:
  name: Delete Invitation
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseURL}}{{organizationsAPI}}1/{{invitationsPath}}30/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
