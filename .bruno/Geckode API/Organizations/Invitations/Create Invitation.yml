info:
  name: Create Invitation
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}{{organizationsAPI}}1/{{invitationsPath}}"
  body:
    type: json
    data: "{\r

      \    \"invitee_id\": 2,\r

      \    \"permission\": \"view\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
