info:
  name: Create Organization
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}{{organizationsAPI}}"
  body:
    type: json
    data: "{\r

      \    \"slug\": \"test-org-1\",\r

      \    \"name\": \"Test Organization 1\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
