info:
  name: Join Organization
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseURL}}{{organizationsAPI}}1/{{joinPath}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
