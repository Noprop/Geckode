info:
  name: Update Organization
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseURL}}{{organizationsAPI}}1/"
  body:
    type: json
    data: "{\r

      \    \"owner\": 1,\r

      \    \"is_public\": 1\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
