info:
  name: Add Organization Project
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}{{organizationsAPI}}1/{{projectsPath}}"
  body:
    type: json
    data: "{\r

      \    \"project_id\": 1,\r

      \    \"permission\": \"code\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
