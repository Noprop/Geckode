info:
  name: Get JWT Token
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}/api/token/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
