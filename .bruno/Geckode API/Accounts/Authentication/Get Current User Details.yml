info:
  name: Get Current User Details
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}{{usersAPI}}user-details/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
