info:
  name: Create User
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}{{usersAPI}}"
  body:
    type: json
    data: |-
      {
          "username": "test4",
          "email": "test4@testing.test",
          "first_name": "Test4",
          "last_name": "Testing4",
          "password": "{{password}}",
          "password2": "{{password}}"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
