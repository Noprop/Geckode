info:
  name: Get Project Group
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{projectGroupsAPI}}1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
