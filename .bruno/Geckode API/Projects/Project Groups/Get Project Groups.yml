info:
  name: Get Project Groups
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}{{projectGroupsAPI}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
