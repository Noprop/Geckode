info:
  name: Create Project
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseURL}}{{projectsAPI}}"
  body:
    type: json
    data: |-
      {
          "name": "my_test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
