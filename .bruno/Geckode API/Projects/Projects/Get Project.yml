info:
  name: Get Project
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{projectsAPI}}1/"
  params:
    - name: search
      value: test
      type: query
      disabled: true
    - name: is_published
      value: "1"
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
