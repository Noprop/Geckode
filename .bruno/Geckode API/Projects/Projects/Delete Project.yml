info:
  name: Delete Project
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}{{projectsAPI}}17/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
