info:
  name: Check User Project Permission
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseURL}}{{projectsAPI}}1/check-user-permission/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
