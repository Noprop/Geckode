info:
  name: Get Asset (Public)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}{{spritesAPI}}8/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
