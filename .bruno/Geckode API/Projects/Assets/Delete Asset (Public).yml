info:
  name: Delete Asset (Public)
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseURL}}{{spritesAPI}}13/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
