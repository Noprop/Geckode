info:
  name: Delete Asset
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseURL}}{{projectsAPI}}18/{{spritesPath}}2/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
