info:
  name: Get Assets (Public)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{spritesAPI}}"
  params:
    - name: search
      value: test2
      type: query
      disabled: true
    - name: ordering
      value: -id
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
