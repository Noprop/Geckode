info:
  name: Create Asset (Public)
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseURL}}{{spritesAPI}}"
  body:
    type: multipart-form
    data:
      - name: name
        type: text
        value: bb chicken
      - name: texture_file
        type: file
        value:
          - /home/jonathan/Work/Capstone/Geckode/frontend/public/sample-sprites/onion.png
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
