info:
  name: Get Asset
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}{{projectsAPI}}18/{{spritesPath}}2/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
