info:
  name: Get Collaborator
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{projectsAPI}}3/{{collaboratorsPath}}2/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
