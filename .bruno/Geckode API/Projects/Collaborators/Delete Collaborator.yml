info:
  name: Delete Collaborator
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseURL}}{{projectsAPI}}1/{{collaboratorsPath}}1/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
