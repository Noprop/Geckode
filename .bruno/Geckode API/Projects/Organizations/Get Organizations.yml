info:
  name: Get Organizations
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseURL}}{{projectsAPI}}33/organizations"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
