info:
  name: Get Invitation
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{projectsAPI}}1/{{invitationsPath}}28/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
