info:
  name: Get Public Sprite Libraries
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseURL}}{{spritesLibrariesAPI}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
