info:
  name: Get Sprite Library
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseURL}}{{projectsAPI}}13/{{spriteLibrariesPath}}5"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
