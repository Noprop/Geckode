info:
  name: Get Sprite (Public)
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseURL}}{{spritesLibrariesAPI}}1/{{spritesPath}}2/"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
